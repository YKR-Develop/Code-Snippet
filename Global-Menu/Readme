HTML / CSS(Sass) / JavaScriptのテンプレートファイルです。
使用される前に以下の点にご注意ください。
・ VSCodeでの使用を想定しています。
・ 個人でコーディングされる方向けのテンプレートファイルです。
・ Sassを使用していますので、ご利用の際には　「Live Sass Compiler」　かターミナルを使用してください。

「Live Sass Compiler」 を使うときには、以下の設定を行って下さい。
1. VSCodeの「管理」から「設定」をクリック
2. 検索欄に "Live Sass Compile" と入力し、Live Sass Compile › Settings: Formatsから「settings.jsonで編集する」をクリック
3. formats内のcssのコンパイル先を以下のように書き換えてください。

"liveSassCompile.settings.formats": [
        {
            "format": "expanded",
            "extensionName": ".css",
            "savePath": "~/../css",
            "savePathReplacementPairs": null
        }
    ],

stylesheetフォルダ → cssフォルダ → style.css
                   → scssフォルダ → style.scss → _variables.scss
というディレクトリ構造になります。
